# Azure Environment Examples

Use `terraform.tfvars.example` files in each stack as the public contract for inputs. In CI, pass real values through protected GitHub environments and Azure federated credentials.
